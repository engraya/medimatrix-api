# Changelog

## Unreleased

- Authored the MediMatrix backend, database schema and migration, domain endpoints, adapters, outbox worker and tests.
- Added development and production Docker configuration, image publishing workflow, operator guides and frontend integration example.
- Installed dependencies, applied local migrations/seed, and verified typecheck, lint, build, unit/integration tests, API startup and database backup.
- Updated runtime/CI/Docker to Node 24, patched audited dependencies, corrected MinIO image registry, and made backup/restore npm commands work in PowerShell.
- Frontend cutover, deployment and a restore drill remain operator tasks. This entry is not a production release certification.
